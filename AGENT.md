# AGENT.md

**→ Read [`AGENTS.md`](AGENTS.md) — the canonical agent file for this repository.**

It serves both missions an AI coding agent is given here:

- **Operate the monitor** (Part 1) — interview-driven install, wire `monitor:read`,
  start/stop/status, upgrade via `scripts/agent-status.sh` / `agent-upgrade.sh`.
- **Develop in this repository** (Part 2) — commands, architecture, invariants.

This file exists so agents that look for `AGENT.md` find their way. All content
lives in `AGENTS.md`.
